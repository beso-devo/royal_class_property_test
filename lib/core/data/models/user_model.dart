/// Here we can do the json serialization but I used the entities directly since
/// we can implement it like the [BaseResponseModel] class.

class UserModel {
  final int id;

  UserModel(this.id);
}
